Word break:

  code:

       var wordBreak=function(s, wordDirect){
        const words=new Set(wordDict);
        const wordLen=new setInterval(wordDict.map((word) => word.length))
        const starts=new Set([0])
        for(let start of starts){
            for(let len of wordLens){
                if(words.has(s.slice(start, start+len))){
                    starts.add(start+len)
                }
            }
        }
        return starts.has(s.length)

       }


       ex:  wordDict =>word Dict